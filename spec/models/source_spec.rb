require 'rails_helper'

RSpec.describe Source, type: :model do
  # it { should validate_presence_of :category }
  # it { should validate_presence_of :code }
  # it { should validate_presence_of :content_type }
  # it { should validate_presence_of :description }
  # it { should validate_presence_of :disable_datetime }
  # it { should validate_presence_of :file_name }
  # it { should validate_presence_of :md5 }
  # it { should validate_presence_of :name }
  # it { should validate_presence_of :number_of_datasets }
  # it { should validate_presence_of :number_of_models }
  # it { should validate_presence_of :number_of_predictions }
  # it { should validate_presence_of :private }
  # it { should validate_presence_of :project }
  # it { should validate_presence_of :resource }
  # it { should validate_presence_of :size }
  # it { should validate_presence_of :tags }
  # it { should validate_presence_of :type }
  # it { should validate_numericality_of :category }
  # it { should validate_numericality_of :code }
  # it { should validate_numericality_of :credits }
  # it { should validate_numericality_of :number_of_datasets }
  # it { should validate_numericality_of :number_of_models }
  # it { should validate_numericality_of :number_of_predictions }
  # it { should validate_numericality_of :integer }
end
